---
type: llm
weight: 1
---

- The reply contains a diagram drawn in text (a tree, arrows or boxes in a code block), not only prose.
- The diagram names the working tree, the index (or staging area), the local repository and the remote.
- It shows the direction of travel with the git commands that move a change between them (add, commit, push).

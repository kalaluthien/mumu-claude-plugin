# mumu-compounding evals

Run with `--allow-tools Bash`: the `armed` grader checks the Bash call that
arms the repository, and Bash is a gated tool.

```sh
claude plugin eval mumu-compounding --allow-tools Bash
```
